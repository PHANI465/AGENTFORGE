[project]
name = "agent-runtime"
version = "0.1.0"
description = "AgentForge Agent Runtime — execution loop, tool orchestration, safety enforcement"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "agentforge-common",
]

[tool.uv]
package = false

[tool.uv.sources]
agentforge-common = { workspace = true }
