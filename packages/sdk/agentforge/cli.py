"""CLI entry point: agentforge deploy | eval | logs.

Populated alongside the corresponding milestones. Stubbed for now so the
`agentforge` console script installs and runs cleanly.
"""


def main() -> None:
    print("agentforge CLI — not yet implemented (see MILESTONES.md)")


if __name__ == "__main__":
    main()
