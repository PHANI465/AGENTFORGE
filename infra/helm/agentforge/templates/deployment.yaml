{{- range $name, $svc := .Values.services }}
{{- if ne $svc.enabled false }}
{{- $healthPath := eq $name "dashboard" | ternary "/healthz" "/health" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $name }}
  labels:
    {{- include "agentforge.labels" $ | nindent 4 }}
    app.kubernetes.io/name: {{ $name }}
spec:
  replicas: {{ $svc.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $name }}
      app.kubernetes.io/instance: {{ $.Release.Name }}
  template:
    metadata:
      labels:
        {{- include "agentforge.labels" $ | nindent 8 }}
        app.kubernetes.io/name: {{ $name }}
    spec:
      containers:
        - name: {{ $name }}
          image: {{ include "agentforge.image" (dict "global" $.Values.global "svc" $svc) }}
          ports:
            - containerPort: {{ $svc.port }}
          {{- $wantsDemoKey := and (eq $name "api-gateway") $.Values.demo.apiKey }}
          {{- if or $svc.env $svc.secretEnv $wantsDemoKey }}
          env:
            {{- range $key, $value := $svc.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- range $svc.secretEnv }}
            - name: {{ . }}
              valueFrom:
                secretKeyRef:
                  name: agentforge-secrets
                  key: {{ . }}
            {{- end }}
            {{- if $wantsDemoKey }}
            # Public shared demo key — api-gateway serves it at
            # /api/v1/demo/api-key so the dashboard can offer one-click entry.
            - name: DEMO_API_KEY
              value: {{ $.Values.demo.apiKey | quote }}
            {{- end }}
          {{- end }}
          resources:
            {{- toYaml $svc.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: {{ $healthPath }}
              port: {{ $svc.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: {{ $healthPath }}
              port: {{ $svc.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
---
{{- end }}
{{- end }}
